import type { TagPaginationInterface } from './TagList'

const TagPagination = ({
  currentPage,
  totalPage,
  onPageChange,
  maxPage = 5,
}: TagPaginationInterface) => {
  return <div></div>
}

export default TagPagination
